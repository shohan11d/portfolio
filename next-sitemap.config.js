/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://kmdshohan.pages.dev',
    generateRobotsTxt: true,           // (optional) Create robots.txt
    sitemapSize: 7000,                 // Number of URLs per sitemap (default is 5000)
  };
  